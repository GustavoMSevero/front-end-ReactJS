/**
 * Babel Converter (transpilar) código do React para um código que o browser entenda
 * webpack: Para cara tipo de arquivo (.js, .css, .png) eu vou converter o código de uma maneira diferente
 * 
 * Loaders: babel-loader, css-loader, image-loadaer
 */
